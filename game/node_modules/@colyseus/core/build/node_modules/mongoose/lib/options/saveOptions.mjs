import clone_1 from '../helpers/clone.mjs';

class SaveOptions {
  constructor(obj) {
    if (obj == null) {
      return;
    }
    Object.assign(this, clone_1(obj));
  }
}

var saveOptions = SaveOptions;

export default saveOptions;
//# sourceMappingURL=saveOptions.mjs.map

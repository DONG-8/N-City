'use strict';

var areDiscriminatorValuesEqual = require('./areDiscriminatorValuesEqual.js');

/*!
* returns discriminator by discriminatorMapping.value
*
* @param {Schema} schema
* @param {string} value
*/

var getSchemaDiscriminatorByValue = function getSchemaDiscriminatorByValue(schema, value) {
  if (schema == null || schema.discriminators == null) {
    return null;
  }
  for (const key of Object.keys(schema.discriminators)) {
    const discriminatorSchema = schema.discriminators[key];
    if (discriminatorSchema.discriminatorMapping == null) {
      continue;
    }
    if (areDiscriminatorValuesEqual(discriminatorSchema.discriminatorMapping.value, value)) {
      return discriminatorSchema;
    }
  }
  return null;
};

module.exports = getSchemaDiscriminatorByValue;
//# sourceMappingURL=getSchemaDiscriminatorByValue.js.map

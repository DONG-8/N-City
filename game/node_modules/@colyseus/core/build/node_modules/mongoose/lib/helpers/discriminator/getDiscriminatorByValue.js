'use strict';

var areDiscriminatorValuesEqual = require('./areDiscriminatorValuesEqual.js');

/*!
* returns discriminator by discriminatorMapping.value
*
* @param {Model} model
* @param {string} value
*/

var getDiscriminatorByValue = function getDiscriminatorByValue(discriminators, value) {
  if (discriminators == null) {
    return null;
  }
  for (const name of Object.keys(discriminators)) {
    const it = discriminators[name];
    if (
      it.schema &&
      it.schema.discriminatorMapping &&
      areDiscriminatorValuesEqual(it.schema.discriminatorMapping.value, value)
    ) {
      return it;
    }
  }
  return null;
};

module.exports = getDiscriminatorByValue;
//# sourceMappingURL=getDiscriminatorByValue.js.map

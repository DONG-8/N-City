'use strict';

var index = require('./index.js');

/*!
 * Module dependencies.
 */




class ObjectExpectedError extends index {
  /**
   * Strict mode error constructor
   *
   * @param {string} type
   * @param {string} value
   * @api private
   */
  constructor(path, val) {
    const typeDescription = Array.isArray(val) ? 'array' : 'primitive value';
    super('Tried to set nested object field `' + path +
      `\` to ${typeDescription} \`` + val + '` and strict mode is set to throw.');
    this.path = path;
  }
}

Object.defineProperty(ObjectExpectedError.prototype, 'name', {
  value: 'ObjectExpectedError'
});

var objectExpected = ObjectExpectedError;

module.exports = objectExpected;
//# sourceMappingURL=objectExpected.js.map

import { commonjsGlobal } from '../../../_virtual/_commonjsHelpers.js';
import assert from 'assert';
import mquery from '../../mquery/lib/mquery.mjs';

/*!
 * ignore
 */




/**
 * Helper for multiplexing promise implementations
 *
 * @api private
 */

const store = {
  _promise: null
};

/**
 * Get the current promise constructor
 *
 * @api private
 */

store.get = function() {
  return store._promise;
};

/**
 * Set the current promise constructor
 *
 * @api private
 */

store.set = function(lib) {
  assert.ok(typeof lib === 'function',
    `mongoose.Promise must be a function, got ${lib}`);
  store._promise = lib;
  mquery.Promise = lib;
};

/*!
 * Use native promises by default
 */

store.set(commonjsGlobal.Promise);

var promise_provider = store;

export default promise_provider;
//# sourceMappingURL=promise_provider.mjs.map

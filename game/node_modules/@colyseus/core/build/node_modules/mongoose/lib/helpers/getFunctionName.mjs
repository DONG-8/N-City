var getFunctionName = function(fn) {
  if (fn.name) {
    return fn.name;
  }
  return (fn.toString().trim().match(/^function\s*([^\s(]+)/) || [])[1];
};

export default getFunctionName;
//# sourceMappingURL=getFunctionName.mjs.map

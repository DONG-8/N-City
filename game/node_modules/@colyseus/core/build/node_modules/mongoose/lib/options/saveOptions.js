'use strict';

var clone = require('../helpers/clone.js');

class SaveOptions {
  constructor(obj) {
    if (obj == null) {
      return;
    }
    Object.assign(this, clone(obj));
  }
}

var saveOptions = SaveOptions;

module.exports = saveOptions;
//# sourceMappingURL=saveOptions.js.map

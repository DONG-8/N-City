import { commonjsGlobal } from '../../../../_virtual/_commonjsHelpers.js';
import map$1 from '../types/map.mjs';
import SchemaMapOptions_1 from '../options/SchemaMapOptions.mjs';
import schematype from '../schematype.mjs';

/*!
 * ignore
 */




/*!
 * ignore
 */

class Map extends schematype {
  constructor(key, options) {
    super(key, options, 'Map');
    this.$isSchemaMap = true;
  }

  set(option, value) {
    return schematype.set(option, value);
  }

  cast(val, doc, init) {
    if (val instanceof map$1) {
      return val;
    }

    const path = this.path;

    if (init) {
      const map = new map$1({}, path, doc, this.$__schemaType);

      if (val instanceof commonjsGlobal.Map) {
        for (const key of val.keys()) {
          let _val = val.get(key);
          if (_val == null) {
            _val = map.$__schemaType._castNullish(_val);
          } else {
            _val = map.$__schemaType.cast(_val, doc, true, null, { path: path + '.' + key });
          }
          map.$init(key, _val);
        }
      } else {
        for (const key of Object.keys(val)) {
          let _val = val[key];
          if (_val == null) {
            _val = map.$__schemaType._castNullish(_val);
          } else {
            _val = map.$__schemaType.cast(_val, doc, true, null, { path: path + '.' + key });
          }
          map.$init(key, _val);
        }
      }

      return map;
    }

    return new map$1(val, path, doc, this.$__schemaType);
  }

  clone() {
    const schematype = super.clone();

    if (this.$__schemaType != null) {
      schematype.$__schemaType = this.$__schemaType.clone();
    }
    return schematype;
  }
}

Map.prototype.OptionsConstructor = SchemaMapOptions_1;

Map.defaultOptions = {};

var map = Map;

export default map;
//# sourceMappingURL=map.mjs.map

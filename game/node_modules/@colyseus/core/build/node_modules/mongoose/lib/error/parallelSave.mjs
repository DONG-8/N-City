import error from './index.mjs';

/*!
 * Module dependencies.
 */



class ParallelSaveError extends error {
  /**
   * ParallelSave Error constructor.
   *
   * @param {Document} doc
   * @api private
   */
  constructor(doc) {
    const msg = 'Can\'t save() the same doc multiple times in parallel. Document: ';
    super(msg + doc._id);
  }
}

Object.defineProperty(ParallelSaveError.prototype, 'name', {
  value: 'ParallelSaveError'
});

/*!
 * exports
 */

var parallelSave = ParallelSaveError;

export default parallelSave;
//# sourceMappingURL=parallelSave.mjs.map

'use strict';

const specialKeys = new Set([
  '$ref',
  '$id',
  '$db'
]);

var isOperator = function isOperator(path) {
  return path.startsWith('$') && !specialKeys.has(path);
};

module.exports = isOperator;
//# sourceMappingURL=isOperator.js.map

function isPromise(val) {
  return !!val && (typeof val === 'object' || typeof val === 'function') && typeof val.then === 'function';
}

var isPromise_1 = isPromise;

export default isPromise_1;
//# sourceMappingURL=isPromise.mjs.map

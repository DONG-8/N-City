import util from 'util';
import redisError from './redisError.mjs';

var ADD_STACKTRACE = false;

function ReplyError (message) {
  var tmp = Error.stackTraceLimit;
  Error.stackTraceLimit = 2;
  redisError.call(this, message, ADD_STACKTRACE);
  Error.captureStackTrace(this, ReplyError);
  Error.stackTraceLimit = tmp;
}

util.inherits(ReplyError, redisError);

Object.defineProperty(ReplyError.prototype, 'name', {
  value: 'ReplyError',
  configurable: true,
  writable: true
});

var replyError = ReplyError;

export default replyError;
//# sourceMappingURL=replyError.mjs.map

'use strict';

var hiredis = require('hiredis');
var replyError = require('./replyError.js');
var parserError = require('./parserError.js');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var hiredis__default = /*#__PURE__*/_interopDefaultLegacy(hiredis);

/**
 * Parse data
 * @param parser
 * @returns {*}
 */
function parseData (parser, data) {
  try {
    return parser.reader.get()
  } catch (err) {
    // Protocol errors land here
    // Reset the parser. Otherwise new commands can't be processed properly
    parser.reader = new hiredis__default['default'].Reader(parser.options);
    parser.returnFatalError(new parserError(err.message, JSON.stringify(data), -1));
  }
}

/**
 * Hiredis Parser
 * @param options
 * @constructor
 */
function HiredisReplyParser (options) {
  this.returnError = options.returnError;
  this.returnFatalError = options.returnFatalError || options.returnError;
  this.returnReply = options.returnReply;
  this.name = 'hiredis';
  this.options = {
    return_buffers: !!options.returnBuffers
  };
  this.reader = new hiredis__default['default'].Reader(this.options);
}

HiredisReplyParser.prototype.execute = function (data) {
  this.reader.feed(data);
  var reply = parseData(this, data);

  while (reply !== undefined) {
    if (reply && reply.name === 'Error') {
      this.returnError(new replyError(reply.message));
    } else {
      this.returnReply(reply);
    }
    reply = parseData(this, data);
  }
};

/**
 * Reset the parser values to the initial state
 *
 * @returns {undefined}
 */
HiredisReplyParser.prototype.reset = function () {
  this.reader = new hiredis__default['default'].Reader(this.options);
};

var hiredis_1 = HiredisReplyParser;

module.exports = hiredis_1;
//# sourceMappingURL=hiredis.js.map

'use strict';

var env = require('../env.js');
var node = require('./node.js');
var collection$1 = require('./collection.js');

if ('unknown' == env.type) {
  throw new Error('Unknown environment');
}

var collection =
  env.isNode ? node :
    env.isMongo ? collection$1 :
      collection$1;

module.exports = collection;
//# sourceMappingURL=index.js.map

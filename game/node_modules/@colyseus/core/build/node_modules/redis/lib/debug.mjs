import redis from '../index.mjs';

function debug () {
    if (redis.debug_mode) {
        console.error.apply(null, arguments);
    }
}

var debug_1 = debug;

export default debug_1;
//# sourceMappingURL=debug.mjs.map

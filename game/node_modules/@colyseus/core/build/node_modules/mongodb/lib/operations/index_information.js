'use strict';

var operation = require('./operation.js');
var common_functions = require('./common_functions.js');

const OperationBase = operation.OperationBase;
const indexInformation = common_functions.indexInformation;

class IndexInformationOperation extends OperationBase {
  constructor(db, name, options) {
    super(options);

    this.db = db;
    this.name = name;
  }

  execute(callback) {
    const db = this.db;
    const name = this.name;
    const options = this.options;

    indexInformation(db, name, options, callback);
  }
}

var index_information = IndexInformationOperation;

module.exports = index_information;
//# sourceMappingURL=index_information.js.map

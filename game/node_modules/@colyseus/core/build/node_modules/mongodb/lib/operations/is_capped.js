'use strict';

var options_operation = require('./options_operation.js');
var utils = require('../utils.js');

const handleCallback = utils.handleCallback;

class IsCappedOperation extends options_operation {
  constructor(collection, options) {
    super(collection, options);
  }

  execute(callback) {
    super.execute((err, document) => {
      if (err) return handleCallback(callback, err);
      handleCallback(callback, null, !!(document && document.capped));
    });
  }
}

var is_capped = IsCappedOperation;

module.exports = is_capped;
//# sourceMappingURL=is_capped.js.map

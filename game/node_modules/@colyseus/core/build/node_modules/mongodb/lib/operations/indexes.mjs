import operation from './operation.mjs';
import common_functions from './common_functions.mjs';

const OperationBase = operation.OperationBase;
const indexInformation = common_functions.indexInformation;

class IndexesOperation extends OperationBase {
  constructor(collection, options) {
    super(options);

    this.collection = collection;
  }

  execute(callback) {
    const coll = this.collection;
    let options = this.options;

    options = Object.assign({}, { full: true }, options);
    indexInformation(coll.s.db, coll.collectionName, options, callback);
  }
}

var indexes = IndexesOperation;

export default indexes;
//# sourceMappingURL=indexes.mjs.map

import find_and_modify from './find_and_modify.mjs';

class FindOneAndDeleteOperation extends find_and_modify {
  constructor(collection, filter, options) {
    // Final options
    const finalOptions = Object.assign({}, options);
    finalOptions.fields = options.projection;
    finalOptions.remove = true;

    // Basic validation
    if (filter == null || typeof filter !== 'object') {
      throw new TypeError('Filter parameter must be an object');
    }

    super(collection, filter, finalOptions.sort, null, finalOptions);
  }
}

var find_one_and_delete = FindOneAndDeleteOperation;

export default find_one_and_delete;
//# sourceMappingURL=find_one_and_delete.mjs.map
